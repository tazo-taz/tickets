/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // webpackDevMiddleware: confing => {
  //   confing.watchOptions = {
  //     poll: 1000,
  //     aggregateTimeout: 300,
  //   };
  //   return confing;
  // }
};

export default nextConfig;
